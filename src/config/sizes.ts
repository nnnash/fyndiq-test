const breakpoints = {
  mobile: 768,
  tablet: 1280,
}

export const MOBILE = `@media (max-width: ${breakpoints.mobile}px)`
export const TABLET = `@media (max-width: ${breakpoints.tablet}px)`

export default breakpoints
